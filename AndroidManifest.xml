<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.myfirstapp.mrpc.phc_project">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity" />
        <activity android:name=".Login" />
        <activity
            android:name=".SplashScreen"
            android:theme="@style/AppTheme.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />

            </intent-filter>
        </activity>
        <activity android:name=".Doc_Login"></activity>
        <activity android:name=".Patient_Complain"></activity>
        <activity android:name=".RealAllComplains"></activity>
        <activity android:name=".Doctorresponse" />
        <activity android:name=".ShowDoctorProfile" />
        <activity
            android:name=".PatientDashboardTabbed"
            android:label="@string/title_activity_patient_dashboard_tabbed"
            android:theme="@style/AppTheme.NoActionBar">

        </activity>
    </application>

</manifest>